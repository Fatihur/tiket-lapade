# Alur Sistem Aplikasi Tiket Wisata Online

## 📊 Diagram Alur Utama

```
┌─────────────┐
│ PENGUNJUNG  │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  1. LANDING PAGE                        │
│  - Lihat daftar wisata                  │
│  - Lihat detail wisata & galeri         │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  2. FORM PEMESANAN                      │
│  - Isi data diri                        │
│  - Pilih jumlah tiket                   │
│  - Lihat total harga                    │
│  - Lihat info rekening                  │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  3. TRANSFER PEMBAYARAN                 │
│  - Transfer ke rekening                 │
│  - Upload bukti transfer                │
│  - Submit pemesanan                     │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  4. HALAMAN SUKSES                      │
│  - Terima kode pemesanan                │
│  - Status: Menunggu Validasi            │
└──────┬──────────────────────────────────┘
       │
       │
       ▼
┌─────────────┐
│    ADMIN    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  5. VALIDASI TRANSAKSI                  │
│  - Lihat daftar pemesanan               │
│  - Cek bukti transfer                   │
│  - Validasi: Valid / Tidak Valid        │
└──────┬──────────────────────────────────┘
       │
       ▼ (Jika Valid)
┌─────────────────────────────────────────┐
│  6. GENERATE QR CODE                    │
│  - Sistem generate QR Code otomatis     │
│  - Buat tiket untuk setiap pengunjung   │
│  - Kirim email (opsional)               │
└──────┬──────────────────────────────────┘
       │
       │
       ▼
┌─────────────┐
│ PENGUNJUNG  │
│  DATANG     │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  7. TUNJUKKAN TIKET                     │
│  - Bawa tiket (cetak/digital)           │
│  - Tunjukkan QR Code                    │
└──────┬──────────────────────────────────┘
       │
       │
       ▼
┌─────────────┐
│  PETUGAS    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  8. SCAN QR CODE                        │
│  - Scan dengan kamera                   │
│  - Atau input manual                    │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  9. VALIDASI TIKET                      │
│  ✓ Valid: Belum digunakan → Izinkan     │
│  ✗ Invalid: Sudah digunakan → Tolak     │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  10. TIKET TERPAKAI                     │
│  - Tandai sudah_digunakan = true        │
│  - Simpan waktu scan                    │
│  - Tidak bisa digunakan lagi            │
└─────────────────────────────────────────┘
```

## 🔄 Alur Detail Per Role

### 1️⃣ ADMIN

```
LOGIN
  │
  ▼
DASHBOARD
  ├─► Lihat statistik
  ├─► Pemesanan terbaru
  └─► Quick actions
  │
  ├─► KELOLA WISATA
  │     ├─► Tambah wisata baru
  │     ├─► Edit data wisata
  │     ├─► Upload galeri foto
  │     └─► Hapus wisata
  │
  ├─► KELOLA TRANSAKSI
  │     ├─► Lihat daftar pemesanan
  │     ├─► Filter by status/tanggal
  │     ├─► Detail transaksi
  │     ├─► Lihat bukti transfer
  │     └─► Validasi pembayaran
  │           ├─► Valid → Generate QR Code
  │           └─► Tidak Valid → Tolak
  │
  ├─► KELOLA USER
  │     ├─► Tambah user/petugas
  │     ├─► Edit user
  │     ├─► Set role & permission
  │     └─► Aktif/nonaktifkan user
  │
  └─► LAPORAN
        ├─► Filter by periode
        ├─► Total pendapatan
        ├─► Total tiket terjual
        └─► Export (opsional)
```

### 2️⃣ PETUGAS TIKET

```
LOGIN
  │
  ▼
DASHBOARD
  ├─► Total scan hari ini
  └─► Riwayat scan terbaru
  │
  ├─► SCAN TIKET
  │     ├─► Aktifkan kamera
  │     ├─► Scan QR Code
  │     │     ├─► Berhasil → Validasi
  │     │     └─► Gagal → Input manual
  │     │
  │     └─► VALIDASI
  │           ├─► Cek kode tiket di database
  │           ├─► Cek sudah_digunakan?
  │           │     ├─► Belum → Izinkan masuk
  │           │     │           └─► Update status
  │           │     └─► Sudah → Tolak
  │           └─► Tampilkan hasil
  │
  └─► RIWAYAT SCAN
        ├─► Lihat semua tiket yang discan
        ├─► Filter by tanggal
        └─► Detail tiket
```

### 3️⃣ BENDAHARA

```
LOGIN
  │
  ▼
DASHBOARD
  ├─► Laporan transaksi valid
  ├─► Filter by periode
  ├─► Total pendapatan
  └─► Detail per transaksi
  │
  └─► VERIFIKASI
        ├─► Cocokkan dengan rekening bank
        ├─► Verifikasi jumlah & nominal
        └─► Buat laporan keuangan
```

### 4️⃣ OWNER

```
LOGIN
  │
  ▼
DASHBOARD
  ├─► STATISTIK HARI INI
  │     ├─► Pendapatan hari ini
  │     └─► Pengunjung hari ini
  │
  ├─► STATISTIK BULAN INI
  │     ├─► Pendapatan bulan ini
  │     └─► Pengunjung bulan ini
  │
  └─► GRAFIK
        ├─► Pilih periode (harian/mingguan/bulanan)
        ├─► Grafik pendapatan
        └─► Grafik pengunjung
```

## 🔐 Alur Keamanan

```
┌─────────────────────────────────────────┐
│  AUTHENTICATION                         │
├─────────────────────────────────────────┤
│  1. User input email & password         │
│  2. Sistem cek di database              │
│  3. Verify password (bcrypt)            │
│  4. Cek status aktif                    │
│  5. Create session                      │
│  6. Redirect by role                    │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  AUTHORIZATION                          │
├─────────────────────────────────────────┤
│  1. Middleware CheckRole                │
│  2. Cek user login?                     │
│  3. Cek role user                       │
│  4. Match dengan required role?         │
│     ├─► Yes → Allow access              │
│     └─► No → 403 Forbidden              │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  QR CODE SECURITY                       │
├─────────────────────────────────────────┤
│  1. Generate unique code                │
│  2. Create QR Code image                │
│  3. Save to database                    │
│  4. Flag: sudah_digunakan = false       │
│  5. Saat scan:                          │
│     ├─► Cek kode valid?                 │
│     ├─► Cek sudah_digunakan?            │
│     └─► Update flag = true              │
│  6. Tidak bisa digunakan lagi           │
└─────────────────────────────────────────┘
```

## 💾 Alur Data

```
┌──────────────┐
│  PENGUNJUNG  │
└──────┬───────┘
       │ Input data pemesanan
       ▼
┌──────────────────┐
│  TABEL PEMESANAN │ ◄─── Admin validasi
└──────┬───────────┘
       │ Status: Valid
       ▼
┌──────────────────┐
│   TABEL TIKET    │ ◄─── Generate QR Code
└──────┬───────────┘
       │ Scan QR Code
       ▼
┌──────────────────┐
│  UPDATE STATUS   │ ◄─── Petugas scan
│  sudah_digunakan │
└──────────────────┘
```

## 📈 Alur Laporan

```
┌─────────────────────────────────────────┐
│  DATA TRANSAKSI                         │
│  (status_pembayaran = 'valid')          │
└──────┬──────────────────────────────────┘
       │
       ├─► ADMIN
       │     └─► Laporan penjualan
       │           ├─► Filter periode
       │           ├─► Total pendapatan
       │           └─► Total tiket
       │
       ├─► BENDAHARA
       │     └─► Laporan transaksi
       │           ├─► Verifikasi data
       │           └─► Laporan keuangan
       │
       └─► OWNER
             └─► Dashboard statistik
                   ├─► Grafik pendapatan
                   ├─► Grafik pengunjung
                   └─► Analisis trend
```

## 🎯 Status Pemesanan

```
MENUNGGU
   │
   ├─► Admin Validasi
   │
   ├─► VALID
   │     ├─► Generate QR Code
   │     ├─► Kirim email (opsional)
   │     └─► Pengunjung bisa masuk
   │
   └─► TIDAK VALID
         ├─► Catatan admin
         └─► Pengunjung tidak bisa masuk
```

## 🎫 Status Tiket

```
BELUM DIGUNAKAN
   │
   ├─► Petugas Scan
   │
   └─► SUDAH DIGUNAKAN
         ├─► Simpan waktu scan
         ├─► Simpan petugas yang scan
         └─► Tidak bisa digunakan lagi
```

## 📧 Alur Email (Opsional)

```
┌─────────────────────────────────────────┐
│  Admin Validasi = Valid                 │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Generate QR Code                       │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Create PDF Tiket                       │
│  - Info pemesanan                       │
│  - QR Code                              │
│  - Barcode (opsional)                   │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Kirim Email                            │
│  To: email_pemesan                      │
│  Subject: Tiket Wisata                  │
│  Attachment: PDF                        │
└─────────────────────────────────────────┘
```

## 🔄 Alur Error Handling

```
┌─────────────────────────────────────────┐
│  ERROR TERJADI                          │
└──────┬──────────────────────────────────┘
       │
       ├─► Validation Error
       │     └─► Tampilkan pesan error
       │           └─► User perbaiki input
       │
       ├─► Authentication Error
       │     └─► Redirect ke login
       │
       ├─► Authorization Error
       │     └─► 403 Forbidden
       │
       ├─► Database Error
       │     └─► Log error
       │           └─► Tampilkan pesan umum
       │
       └─► File Upload Error
             └─► Validasi gagal
                   └─► Tampilkan pesan error
```

---

**Catatan**: Alur ini dapat disesuaikan dengan kebutuhan spesifik masing-masing implementasi.
